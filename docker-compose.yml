services:
  outlook-mcp:
    image: ghcr.io/leonine-studios/useful-outlook-mcp:latest
    env_file:
      - .env
    environment:
      - PORT=3000
      - NODE_ENV=production
    ports:
      - "${PORT:-3000}:3000"
    restart: unless-stopped
